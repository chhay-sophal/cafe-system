{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "customer-display",
  "description": "Capability for the customer-facing display window - read-only, needs only the core event bus to receive cart/sale updates from the main window",
  "windows": ["customer-display"],
  "permissions": [
    "core:default"
  ]
}
